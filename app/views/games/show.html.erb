<%= render partial: 'card', locals: {game: @game} %>

<% if @game.has_developer?(current_user) %>
    <%= button_to "Edit Game", edit_game_path(@game), :method => :get %>
    <%= form_tag(@game, :method => :delete) do %>
        <%= submit_tag "Delete" %>
    <% end %>
<% end %>
